export class LinkedinHandler {
    constructor(){}
    
    // Linkedin doesn't allow for automatic job search
    // it is against their rules
    // any user who attempts to do this will get a ban (they might also ban IP)
    static getAllListings(): any {
        return 'Linkedin offer test response'
    }
}